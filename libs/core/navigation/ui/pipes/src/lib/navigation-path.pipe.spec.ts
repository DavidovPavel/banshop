import { NavigationPathPipe } from './navigation-path.pipe';

describe('NavigationPathPipe', () => {
  it('create an instance', () => {
    const pipe = new NavigationPathPipe();
    expect(pipe).toBeTruthy();
  });
});
