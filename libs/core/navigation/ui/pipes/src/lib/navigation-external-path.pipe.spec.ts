import { NavigationExternalPathPipe } from './navigation-external-path.pipe';

describe('NavigationExternalPathPipe', () => {
  it('create an instance', () => {
    const pipe = new NavigationExternalPathPipe();
    expect(pipe).toBeTruthy();
  });
});
